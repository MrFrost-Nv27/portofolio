# Runtime data (SQLite DB + admin-uploaded content) — not committed
/data/
/uploads/

# Local environment config — copy .env.example to .env and edit
/.env

# Go
/portfolio-server
*.exe
/tmp/
build-errors.log

# Node / frontend build output (generated by `pnpm build`, embedded by Go)
/node_modules/
/public/build/*
!/public/build/.gitkeep
/auto-imports.d.ts
/components.d.ts

# Editor/OS
.DS_Store
